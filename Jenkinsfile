pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself'
                echo 'I changed this part of Jenkinsfile'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}